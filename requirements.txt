# EVOLV platform dependencies.
# Minimum versions below are SECURITY FLOORS set by the
# 2026-07-16 pip-audit sweep (SEC-10) — do not lower them.
# CI re-audits on every push (.github/workflows/compliance-check.yml).
fastapi>=0.139.2
fastapi-cli
uvicorn
boto3
anthropic
streamlit
python-docx
pypdf>=6.14.2
pdfplumber
pandas
openpyxl
Pillow>=12.3.0
fpdf2
openai
pinecone
pydantic
requests>=2.34.2
# Security floors for transitive deps flagged by pip-audit
starlette>=1.3.1
urllib3>=2.7.0
tornado>=6.5.7
cryptography>=49.0.0
